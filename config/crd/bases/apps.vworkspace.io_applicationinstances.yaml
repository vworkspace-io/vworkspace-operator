---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applicationinstances.apps.vworkspace.io
spec:
  group: apps.vworkspace.io
  names:
    kind: ApplicationInstance
    listKind: ApplicationInstanceList
    plural: applicationinstances
    shortNames:
    - appinst
    singular: applicationinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.chart.name
      name: Chart
      type: string
    - jsonPath: .spec.chart.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ApplicationInstance is the Schema for the applicationinstances
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationInstanceSpec defines the desired state of ApplicationInstance.
            properties:
              appRef:
                properties:
                  catalogId:
                    minLength: 1
                    type: string
                required:
                - catalogId
                type: object
              chart:
                description: Chart is required in managed mode and must be omitted
                  in placeholder mode.
                properties:
                  name:
                    minLength: 1
                    type: string
                  sourceType:
                    description: ChartSourceType identifies how the chart is fetched.
                    enum:
                    - oci
                    - helm
                    type: string
                  url:
                    minLength: 1
                    type: string
                  version:
                    minLength: 1
                    type: string
                required:
                - name
                - sourceType
                - url
                - version
                type: object
              integrations:
                properties:
                  certManager:
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  externalSecrets:
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  ingress:
                    properties:
                      enabled:
                        type: boolean
                      host:
                        type: string
                    type: object
                type: object
              mode:
                default: managed
                description: Mode selects the reconcile strategy. Empty defaults to
                  "managed".
                enum:
                - managed
                - placeholder
                type: string
              release:
                description: Release is required in managed mode and must be omitted
                  in placeholder mode.
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              values:
                description: Values is required in managed mode and must be omitted
                  in placeholder mode.
                properties:
                  configMapRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  inline:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  secretRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  source:
                    description: ValuesSource identifies where chart values come from.
                    enum:
                    - inline
                    - secretRef
                    - configMapRef
                    type: string
                required:
                - source
                type: object
            required:
            - appRef
            type: object
          status:
            description: ApplicationInstanceStatus defines the observed state of ApplicationInstance.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                items:
                  properties:
                    name:
                      type: string
                    notes:
                      type: string
                    type:
                      type: string
                    url:
                      type: string
                  required:
                  - name
                  - url
                  type: object
                type: array
              helmReleaseRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              lastAppliedChart:
                properties:
                  name:
                    type: string
                  sourceType:
                    description: ChartSourceType identifies how the chart is fetched.
                    enum:
                    - oci
                    - helm
                    type: string
                  url:
                    type: string
                  version:
                    type: string
                required:
                - name
                - sourceType
                - url
                - version
                type: object
              lastReconcileTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
